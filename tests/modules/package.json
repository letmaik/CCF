{
  "private": true,
  "scripts": {
    "build": "del-cli --force dist/ && rollup --config",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "lodash-es": "^4.17.15"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^8.4.0",
    "del-cli": "^3.0.1",
    "rollup": "^2.23.0"
  }
}
